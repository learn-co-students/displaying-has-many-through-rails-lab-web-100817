<% @patient.appointments.each do |appt| %>
  <p><%= appt.appointment_datetime.strftime("%B %d, %Y") %> at <%= appt.appointment_datetime.strftime("%H:%M") %></p>
  <p><%= link_to appt.doctor.name, doctor_path(appt.doctor) %></p>
  <% end %>
